import { TCategory, TProduct } from "@/lib/types";
import React from "react";

type TProps = {
  moduleData: TCategory[];
};

const BannerModule = ({ moduleData }: TProps) => {
  return <div>BannerModule</div>;
};

export default BannerModule;
